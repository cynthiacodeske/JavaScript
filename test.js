function calculator (number1, number2) {
    let addition = number1 + number2;
    let subtraction = number1 - number2;
    let division = number1 / number2;
    let multiplication = number1 * number2;
    let modulas = number1 % number2;

    console.log ("The sum is", + addition)
    console.log ("The difference is", + subtraction)
    console.log ("The division is", + division)
    console.log ("The product", + multiplication)
    console.log ("The modulas", + modulas)
}

calculator(20, 15);

